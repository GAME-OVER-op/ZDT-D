# Pinned upstream for the qWDTT transport. Machine-readable: KEY=VALUE.
# fetch-and-build.sh reads this file. Do not add secrets here.

URL=https://github.com/SpaceNeuroX/proxy-turn-vk-android.git

# Pinned to upstream HEAD (tagged v1.3.8 / v1.3.9 / v1.4.0 / v1.4.0-beta -- all
# the same commit). Verified: go_client compiles and both of our patches apply.
#
# History: we were previously pinned to 7b5dcec because the then-HEAD 4791f8c did
# not build (group.go referenced workerErrorHint, defined nowhere). That is fixed
# upstream -- the helper now lives in go_client/errhint.go -- so the pin moves
# forward. This commit also adds the SOCKS5 client mode (-mode socks / -socks),
# which runs WireGuard in userspace netstack and needs no TUN, amneziawg-go or
# awg; qwdtt-cli exposes it via the `mode` config key.
COMMIT=2dd5d37f18a0475a786a90d69feb7c503e33bdf3

# The subdirectory holding the standalone transport (package main).
SUBDIR=go_client
