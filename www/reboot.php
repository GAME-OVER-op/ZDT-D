<?php
// Проверяем, что запрос идет методом GET (можно использовать POST для большей безопасности)
if ($_SERVER['REQUEST_METHOD'] === 'GET') {
    // Выполняем команду перезагрузки
    exec('svc power reboot', $output, $return_var);

    // Проверяем успешность выполнения команды
    if ($return_var === 0) {
        echo "Устройство перезагружается...";
    } else {
        echo "Ошибка: не удалось выполнить перезагрузку.";
    }
} else {
    echo "Неверный метод запроса.";
}